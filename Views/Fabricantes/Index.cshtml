@model IEnumerable<LivroASPdotNETMVC5.Models.Fabricante>

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=decive-width" />
        <title>Index</title>
    </head>
    <body>
        <p>
            @Html.ActionLink("Novo Fabricante", "Create")
        </p>
        <table class="table">
            <!--Linha-->
            <tr>
                <!--Título-->
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>Opções</th>
            </tr>
            @foreach (var item in Model) {
                <!--Linha-->
                <tr>
                    <!--Conteúdo da Linha-->
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.ActionLink("Alterar", "Edit", new {id = item.FabricanteId}) |
                        @Html.ActionLink("Detalhes", "Details", new {id = item.FabricanteId}) |
                        @Html.ActionLink("Delete", "Delete", new {id = item.FabricanteId})
                    </td>
                </tr>
            }
        </table>
    </body>
</html>